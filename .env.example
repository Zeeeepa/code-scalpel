# Code Scalpel (example env)
# Copy to .env for local development; do not commit .env

# --- Ninja Warrior acceptance harness (opt-in) ---
# Enable the Ninja Warrior acceptance tests (otherwise they skip).
RUN_NINJA_WARRIOR=0

# Enable the heavy Ninja Warrior tests (graph/crawl/map/call-graph).
RUN_NINJA_WARRIOR_HEAVY=0

# Path to the Code-Scalpel-Ninja-Warrior repo.
# If omitted, the tests default to /mnt/k/backup/Develop/Code-Scalpel-Ninja-Warrior.
# NINJA_WARRIOR_ROOT=/absolute/path/to/Code-Scalpel-Ninja-Warrior

# Where to write Ninja Warrior evidence JSON + server logs.
# Default (if unset): code-scalpel/evidence/ninja-warrior/
# NINJA_WARRIOR_EVIDENCE_DIR=/absolute/path/to/evidence/output
